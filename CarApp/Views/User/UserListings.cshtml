@model IEnumerable<CarInfoViewModel>

<div class="d-flex justify-content-between align-items-center mb-3" style="margin-top: 20px;">
    @if (User?.Identity?.IsAuthenticated == true)
    {
        <a asp-controller="CarListings" asp-action="Add" class="btn btn-primary"><i class="fas fa-plus"></i>Add Car Listing</a>
    }
</div>
<hr />
<style>
    .card-img-top {
        width: 100%;
        height: 150px;
        object-fit: cover;
    }

</style>
<div class="row text-center">
    @foreach (var carListing in Model)
    {
        <partial name="_CarListingPartial" model="@carListing" />
        <a asp-controller="User" asp-action="EditListing" asp-route-id="@carListing.Id" class="btn btn-primary m-1 w-100">
            <i class="fas fa-plus"></i>Edit
        </a>
        <a asp-controller="User" asp-action="Delete" asp-route-id="@carListing.Id" class="btn btn-danger m-1 w-100"
           onclick="return confirm('Are you sure you want to delete this product?');">Delete</a>
    }
</div>